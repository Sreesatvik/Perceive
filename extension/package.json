{
  "name": "perceive-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Perceive browser extension — content script, popup, and on-device vision pipeline.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "1.0.1",
    "tesseract.js": "7.0.0"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
